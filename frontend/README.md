# Frontend

Vue 3 + TypeScript frontend for the public knowledge garden review-card app.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Data

The development app reads `/api/cards/today` through the Vite proxy.
Static card JSON can be generated by the backend exporter into `frontend/public/cards/today.json`.
